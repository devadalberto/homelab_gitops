apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-cert
  namespace: observability
spec:
  secretName: grafana-tls
  dnsNames: ["grafana.lab-minikube.labz.home.arpa"]
  issuerRef: { name: labz-ca-issuer, kind: ClusterIssuer }
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prom-cert
  namespace: observability
spec:
  secretName: prom-tls
  dnsNames: ["prom.lab-minikube.labz.home.arpa"]
  issuerRef: { name: labz-ca-issuer, kind: ClusterIssuer }
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alert-cert
  namespace: observability
spec:
  secretName: alert-tls
  dnsNames: ["alert.lab-minikube.labz.home.arpa"]
  issuerRef: { name: labz-ca-issuer, kind: ClusterIssuer }
